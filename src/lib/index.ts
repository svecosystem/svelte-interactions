import { createPress, type PressConfig, type PressResult } from './press.js';
import type { PressEvent, PointerType } from './types/events.js';

export { createPress, type PressEvent, type PressConfig, type PressResult, type PointerType };
